[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit"

# React Router: direct URLs like /about must serve the SPA shell
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
